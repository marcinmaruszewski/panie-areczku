{
    "$schema": "https://opencode.ai/config.json",
    "theme": "nord",
    "agent": {
        "build": {
            "disable": true
        },
        "plan": {
            "disable": true
        }
    }
}
